# Netmiko Connector for Nokia FlexiNS

A netmiko SSH connector for Nokia FlexiNS  

## installation

* extract the `netmiko_nokia.tgz` to netmiko

* modify the `netmiko/ssh_dispatcher.py` to add the connetor.

